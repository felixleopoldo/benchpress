{
    "title": "trilearn_bandmat",
    "description": "Generates a graph with band structured adjacency matrix.",
    "type": "array",
    "items": {
        "title": "trilearn_bandmat item",
        "description": "A graph with a band structured adjacency matrix",
        "properties": {
            "id": {
                "title": "ID",
                "type": "string"
            },
            "dim": {
                "$ref": "#/$defs/flexnonnegint"
            },
            "bandwidth": {
                "$ref": "#/$defs/flexnonnegint"
            }
        },
        "required": [
            "id",
            "dim",
            "bandwidth"
        ],
        "additionalProperties": false,
        "examples": [
            {
                "id": "AR2",
                "bandwidth": 2,
                "dim": 50
            }
        ]
    },
    "uniqueItems": true
}