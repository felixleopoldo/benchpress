{
    "title": "Huge R package",
    "description": "TIGER: A tuning-insensitive approach for optimally estimating Gaussian graphical models",
    "type": "array",
    "items": {
        "title": "",
        "description": "",
        "type": "object",
        "properties": {
            "id": {
                "desciption": "Algorithm identifier",
                "type": "string"
            },
            "timeout": {
                "$ref": "#/$defs/flexnonnegnumnull"
            },
            "lambda": {
                "description": "A positive number to control the regularization. Typical usage is to leave the input lambda: null and have the program compute its own.",
                "$ref": "#/$defs/flexnonnegnum",
                "default": 1
            }
        },
        "required": [
            "id",
            "timeout"
        ],
        "examples": [
            {
                "id": "huge_tiger",
                "timeout": null,
                "lambda": [
                    2,
                    1,
                    0.8,
                    0.6,
                    0.4,
                    0.2,
                    0.1,
                    0.05,
                    0.01
                ]
            }
        ]
    },
    "uniqueItems": true
}