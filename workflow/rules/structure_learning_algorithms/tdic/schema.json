{
    "title": "Some title",
    "description": "List of objects",
    "type": "array",
    "items": {
        "title": "Some item",
        "description": "Some description.",
        "type": "object",
        "properties": {
            "id": {
                "description": "Algorithm identifier",
                "type": "string"
            },
            "timeout": {
                "$ref": "../../../schemas/definitions.schema.json#/definitions/flexnonnegnumnull"
            },
            "mgraph": {
                "description": "If true, augment data with missingness-indicator nodes R_<variable> and forbid edges accordingly (m-graph style). If false, learn structure on substantive variables only.",
                "type": "boolean"
            }
        },
        "required": [
            "id",
            "timeout",
            "mgraph"
        ],
        "examples": [
            {
                "id": "myalg",
                "cutoff": 0.5,
                "timeout": null
            }
        ]
    },
    "uniqueItems": true
}