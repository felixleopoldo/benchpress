{
    "title": "Some title",
    "description": "List of objects",
    "type": "array",
    "items": {
        "title": "Some item",
        "description": "Some description.",
        "type": "object",
        "properties": {
            "id": {
                "description": "Algorithm identifier",
                "type": "string"
            },
            "timeout": {
                "$ref": "../../../schemas/definitions.schema.json#/definitions/flexnonnegnumnull"
            },
            "sepnodes_on_path": {
                "description": "If true, an edge x-y is only removed if pval >= alpha AND all nodes in the separating set lie on some indirect simple path between x and y in the current skeleton.",
                "type": "boolean"
            }
        },
        "required": [
            "id",
            "timeout"
        ],
        "examples": [
            {
                "id": "myalg",
                "cutoff": 0.5,
                "timeout": null,
                "sepnodes_on_path": false
            }
        ]
    },
    "uniqueItems": true
}
